<!--index.wxml-->
<view class="container">
  <!-- map.wxml -->
<map id="map" longitude="{{center.x}}" latitude="{{center.y}}" scale="{{center.scale}}" markers="{{markers}}" bindmarkertap="markertap" bindcallouttap="callouttap" polyline="{{polyline}}" bindregionchange="regionchange" show-location style="width: 100%; height: 100%;"></map>
</view>
